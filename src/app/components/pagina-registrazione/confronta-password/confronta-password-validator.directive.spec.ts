import { ConfrontaPasswordValidator } from './confronta-password-validator.directive';

describe('ConfrontaPasswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfrontaPasswordValidator();
    expect(directive).toBeTruthy();
  });
});
